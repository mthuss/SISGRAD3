#include <stdio.h>
#include <stdlib.h>
#include "funcoes.c"
#define MAX 4 //num max chaves (ou seja, num de ponteiros por pagina = 5)

int main()
{
	FILE *turma, *indices;
	abrirArquivos(&turma, &indices);
//	criaRegistro(turma);
}
